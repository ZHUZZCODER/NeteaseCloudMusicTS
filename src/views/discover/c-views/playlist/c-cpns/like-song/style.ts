import styled from 'styled-components'

export const LikeSongWrapper = styled.div`
  .likeList {
    margin: 20px 0 25px 0;
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;

    * > {
      flex-shrink: 0;
    }
  }
`
